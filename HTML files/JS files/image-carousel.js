document.addEventListener("back-btn", e => {
    e.preventDefault();
    back();
})

document.addEventListener("next-btn", e => {
    e.preventDefault();
    next();
})

function back() {
    
}

function next() {

}